// load the create report form
var report_area = "#report_dialog" ;
<% if is_mobile_device? %>
  $(report_area).html("<%= escape_javascript( render(partial: 'new', :formats => [:mobile]) ) %>") ;
<% else %>
  $(report_area).html("<%= escape_javascript( render partial: 'new' ) %>") ;
<% end %>
